apiVersion: deployments.plural.sh/v1alpha1
kind: ServiceDeployment
metadata:
  name: 27-flux
  namespace: testing
spec:
  namespace: testing
  reconciliation:
    driftDetection: true
    interval: 1m
  repositoryRef:
    name: {{ configuration.repositoryName }}
  helm:
    url:  oci://registry-1.docker.io/bitnamicharts/flux
    version: x.x.x
    chart: flux
  clusterRef:
    kind: Cluster
    name: {{ configuration.cluster }}
    namespace: testing