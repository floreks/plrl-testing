apiVersion: v1
kind: ConfigMap
metadata:
  name: precedence-helm-only
  namespace: testing
  annotations:
    helm.sh/hook: post-install
    helm.sh/hook-weight: "5"
  labels:
    app: precedence-test
    scenario: annotation-precedence
    priority: "lowest"
data:
  info: "only helm.sh/hook-weight annotation - should use weight 5"

