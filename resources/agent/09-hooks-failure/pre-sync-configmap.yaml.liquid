apiVersion: v1
kind: ConfigMap
metadata:
  name: hooks-failure-pre-sync
  namespace: testing
  annotations:
    deployment.plural.sh/sync-hook: pre-sync
    deployment.plural.sh/sync-wave: "0"
  labels:
    scenario: hooks-failure
    phase: pre-sync
    wave: "0"
    app: hooks-failure-test
data:
  note: "prepare environment for failing sync"

