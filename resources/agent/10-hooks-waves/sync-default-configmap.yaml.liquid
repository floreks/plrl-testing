apiVersion: v1
kind: ConfigMap
metadata:
  name: hooks-waves-sync-default
  namespace: testing
  annotations:
    deployment.plural.sh/sync-hook: sync
    # Intentionally omit deployment.plural.sh/sync-wave to test default wave ordering (expected wave 0)
  labels:
    scenario: hooks-waves
    phase: sync
    wave: "0"  # expected controller-assigned default
    app: hooks-waves
    managed-by: plural-test
data:
  note: "sync default wave (no explicit wave annotation)"

