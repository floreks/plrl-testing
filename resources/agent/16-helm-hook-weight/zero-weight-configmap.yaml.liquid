apiVersion: v1
kind: ConfigMap
metadata:
  name: helm-weight-zero
  namespace: testing
  annotations:
    helm.sh/hook: pre-install
    helm.sh/hook-weight: "0"
  labels:
    app: helm-weight-test
    scenario: helm-hook-weight
data:
  info: "helm hook-weight 0 - should execute second"

